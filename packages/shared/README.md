# packages/shared

Shared utilities, helpers, and hooks used by multiple applications or packages.

## Future contents

- Cross-cutting utilities.
- Shared React hooks.
- Common validation and formatting helpers.

## Status

Phase 1 scaffolding only. Code will migrate here from `src/lib/` and `src/hooks/` over time.
