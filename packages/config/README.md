# packages/config

Shared tooling configuration for the PartsPeddle monorepo.

## Contents

- `eslint.config.js` — shared ESLint configuration.
- `prettier.config.js` — shared Prettier configuration.
- `tailwind.config.ts` — shared Tailwind CSS configuration.
- `postcss.config.js` — shared PostCSS configuration.
- `commitlint.config.cjs` — shared commitlint configuration.
- `tsconfig.base.json` — base TypeScript configuration.

## Status

Phase 1 scaffolding. Shared configs will be extracted here from the repository root.
