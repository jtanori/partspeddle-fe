# Unit Tests

Fast, isolated tests for pure functions, domain logic, view-model builders, and store slices.

- No I/O, network, or browser.
- Prefer mocking external dependencies.
- Keep execution time low enough to run on every save.
