// Shared PDP Types
export interface TabViewModel {
  id: string;
  label: string;
  content: any;
}

export interface PartSummaryViewModel {
  id: string;
  title: string;
  price: number;
  imageUrl: string;
}
