{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://partspeddle.com/schemas/delivery.branches.json",
  "title": "Delivery Branches Configuration",
  "description": "Branch names used by the delivery pipeline.",
  "type": "object",
  "required": ["deliveryBranches", "productionBranch", "stagingBranch"],
  "properties": {
    "$schema": { "type": "string" },
    "description": { "type": "string" },
    "deliveryBranches": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Branches that trigger the full delivery workflow including deployment and verification."
    },
    "productionBranch": {
      "type": "string",
      "description": "Branch that triggers production deployments."
    },
    "stagingBranch": {
      "type": "string",
      "description": "Branch that triggers staging deployments."
    },
    "deliveryIntegrationBranch": {
      "type": "string",
      "description": "Temporary branch used to iterate on delivery infrastructure before promoting to develop."
    }
  }
}
